# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# public/data/*.json is gegenereerd, maar staat bewust WEL in git:
# De fetch kan op Netlify falen (ECONNRESET, oorzaak onbevestigd — zie CLAUDE.md).
# De gecommitte dataset dient als fallback zodat de deploy blijft werken. Zie CLAUDE.md.

# geheimen (API-keys) — nooit committen, zie CLAUDE.md
.env
.env.*
!.env.example

# persoonlijke Claude Code-agents, horen niet in de repo
.claude/agents/

# Stadspagina's: build-time gegenereerd door scripts/genereer-gemeentepaginas.ts, dat als
# prebuild en predev draait. Niet committen — ze zouden bij elke dataverversing scheeflopen
# met public/data. De sitemap staat wél in git en het script bewaakt dat ze gelijk blijven.
/gemeente/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
