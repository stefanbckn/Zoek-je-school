# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# public/data/*.json is gegenereerd, maar staat bewust WEL in git:
# De fetch kan op Netlify falen (ECONNRESET, oorzaak onbevestigd — zie CLAUDE.md).
# De gecommitte dataset dient als fallback zodat de deploy blijft werken. Zie CLAUDE.md.

# geheimen (API-keys) — nooit committen, zie CLAUDE.md
.env
.env.*
!.env.example

# persoonlijke Claude Code-agents, horen niet in de repo
.claude/agents/

# Stadspagina's + sitemap: build-time gegenereerd door scripts/genereer-gemeentepaginas.ts, dat
# als prebuild en predev draait. Niet committen — ze zouden bij elke dataverversing scheeflopen
# met public/data. Sinds v2.12.0 genereert hetzelfde script ook public/sitemap.xml uit dezelfde
# lijst als de pagina's, zodat de twee per definitie niet meer uit elkaar kunnen lopen (met 172
# pagina's is een met de hand bijgehouden sitemap geen haalbare vangrail meer).
/gemeente/
/public/sitemap.xml

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
